<% is_mine = rmd_chat_message.user_id == current_user.id %>
<div class="d-flex <%= is_mine ? 'justify-content-end' : 'justify-content-start' %> mb-2">
  <div class="<%= is_mine ? 'bg-primary text-white' : 'bg-light' %> p-2 rounded" style="max-width: 70%;">
    <%= rmd_chat_message.content %>
  </div>
</div>